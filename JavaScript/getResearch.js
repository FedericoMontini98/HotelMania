/* Funzione che si occupa di estrarre la parola ricercata in modo da mantenerla anche cambiando pagina */
function getResearch(){
	var stringa= document.getElementById("ricerca").value;
	return stringa;
}